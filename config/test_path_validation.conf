# Test location path validation
http {
    server {
        listen 8080;
        
        location / {
            root /var/www;
        }
        
        location /api {
            root /var/api;
        }
        
        location /test/ {
            root /var/test;
        }
    }
}
