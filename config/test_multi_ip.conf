# Test multiple IPs on same port (should succeed)
server {
    listen 127.0.0.1:8080;
    listen 192.168.1.1:8080;
    listen 0.0.0.0:8080;
    root /var/www;
    
    location / {
        methods GET;
    }
       location / {
        methods GET;
    }
}
