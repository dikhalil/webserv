# Test return with code only (no URL) - Should PASS
server {
    listen 8080;
    root /var/www;
    
    location /not-found {
        return 404;
    }
    
    location /forbidden {
        return 403;
    }
    
    location /redirect {
        return 301 /new-location;
    }
}
