# Test same IP on multiple ports (should work)
server {
    listen 127.0.0.1:80;
    listen 127.0.0.1:8080;
    listen 127.0.0.1:9000;
    root /var/www;
    
    location / {
        methods GET;
    }
}
