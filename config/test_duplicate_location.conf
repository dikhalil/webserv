# Test duplicate location paths (should use first match)
server {
    listen 8080;
    root /var/www;
    
    location / {
        methods GET POST;
        root /first;
    }
    
    location / {
        methods DELETE;
        root /second;
    }
}
